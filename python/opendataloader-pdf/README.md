# opendataloader-pdf Python Wrapper

This directory contains the Python wrapper package for `opendataloader-pdf`.

The release build copies the repository root `README.md` here before packaging so published metadata stays in sync. This local file exists so editable installs and `uv sync` work during development.

For full project documentation, see the repository root `README.md`.
